# 제출 파이프라인 v2

## 한 줄 요약

긴 공고를 앞에서 잘라 넣는 대신, **모든 첨부문서에서 법 위반 판단에 필요한 원문 조각을 골라 Gemma에 한 번에 보여 주고**, 금액·계약유형·날짜처럼 계산으로 확정되는 판정은 코드가 교정한다.

## 왜 기존 베이스라인보다 유리한가

기존 방식은 공고문 앞 4,000자와 검색된 법령을 주로 사용했다. 이 구조에서는 공고 뒤쪽 참가자격과 규격서·과업지시서·제안요청서가 잘리며, 서로 다른 24개 기준을 모델이 법령 원문에서 다시 찾아 해석해야 한다.

v2는 다음 순서로 동작한다.

1. 공고문과 모든 첨부문서를 470자 이하의 원문 span으로 나눈다.
2. 참가자격, 실적, 지역, 직접생산, 중소기업, 모델명, 공급확약, SW, 공동수급, 설명회, 제출마감 등의 span을 우선 선택한다.
3. 규격서·과업지시서·RFP마다 모델명 후보와 `동등 이상` 예외 문구를 최소 한 번 보존한다.
4. 배포된 경쟁제품 CSV를 10자리 세부품명 코드로 결합하고 `3억원 미만`, `10억원 미만` 같은 특이사항도 계산한다.
5. 24개 항목의 적용조건·위반조건·예외를 짧은 판정 카드로 Gemma에 제공한다.
6. Gemma는 긴 근거문구 대신 `{y, s}`만 출력한다. `s`는 원문 span 번호이므로 근거 환각과 출력 토큰을 줄인다.
7. 확실한 금액·계약·날짜 규칙을 후처리하고, 근거는 실제 원문의 정확한 부분문자열만 CSV에 쓴다.

## 직관적인 예시

### 규격서의 특정 모델명(v9)

공고문이 8,000자이고 20,000자짜리 규격서 뒤쪽에 `모델명: ZX-9000`이 있어도 기존 4,000자 절단 방식은 볼 수 없다. v2는 규격서에서 모델명 span을 별도로 보존하므로 Gemma가 해당 문장과 `동등 이상 허용` 여부를 함께 판단한다.

### 예산보다 큰 최소 실적(v3)

배정예산이 3억 5천만원인데 참가자격이 `단일 건 4억 5천5백만원 이상 수행실적`을 요구하면 코드가 두 금액을 비교해 v3을 1로 고정한다. dev에서는 이 규칙이 TP 8, FP 0이었다.

### 지방 협상계약 설명회 기간(v23)

게시일이 2월 19일, 설명회가 3월 3일, 제안서 마감이 3월 11일이고 추정가격이 2억 9천만원이면 설명회 이후 최소 20일이 필요하지만 실제로는 8일이다. 코드가 날짜를 계산해 v23을 1로 고정한다. 개찰일은 사용하지 않는다.

### 경쟁제품의 금액 조건(v10~v18)

`축제기획및대행서비스`는 고시상 추정가격 3억원 미만만 경쟁제품이다. 추정가격이 3억 2천만원이면 코드가 `금액기준 제외`로 표시해, 모델이 코드 일치만 보고 경쟁제품으로 오판하지 않게 한다.

## 로컬 검증 결과

- 단위·통합 회귀 테스트: 18개 통과 (2026-09-23 출력 복구 수정 포함)
- dev 정답 양성을 확정 0 규칙이 지운 사례: 0개
- 고신뢰 규칙 강제 양성: 27개, dev 기준 FP 0
  - v2 3개, v3 8개, v21 6개, v22 5개, v23 5개
- 제공된 비어 있지 않은 gold 근거의 선택 span 포함률: 54/54
- Gemma 출력을 전부 0으로 둔 규칙층만의 dev Macro F1: `0.191667`

`0.191667`은 실제 모델 성능이 아니라 **규칙층만의 하한 점검값**이다. 최종 성능은 평가 서버의 고정 Gemma 출력이 나머지 19개 항목을 얼마나 잘 판정하는지에 달려 있으므로, 실제 제출 점수를 보장하는 수치는 아니다.

## 실행

평가 서버에서는 ZIP 최상위의 `script.py`가 기본 경로를 사용한다.

```bash
python script.py
```

GPU 없이 입출력 계약만 확인하려면 저장소 루트에서 다음처럼 실행한다.

```bash
python open/baseline/script.py --mock \
  --data-dir open/data \
  --input open/data/test.jsonl.gz \
  --output-dir /tmp/nara-output
```

실제 Gemma 환경에서 dev 평가를 할 때는 다음 옵션을 추가한다.

```bash
python open/baseline/script.py \
  --data-dir open/data \
  --input open/dev.jsonl.gz \
  --output-dir /tmp/nara-dev \
  --labels open/dev_labels.csv
```

## 제출물

루트의 `submit.zip`에는 최상위에 다음 두 파일만 둔다.

- `script.py`
- `requirements.txt`

## 2026-09-23 제출 오류 수정

서버에서 `2회 호출 뒤에도 구조화 출력 불완전` 오류가 보고됐다. 기존 파서는
판정값 `y`가 정상이어도 근거 번호 `s`가 -1~512 범위 밖이면 항목 전체를
누락으로 처리했고, 재시도 후 한 항목이라도 남으면 모든 공고의 CSV 생성을 중단했다.
서버 원본 응답은 없으므로 실제 누락 원인이 근거 번호인지 판정값인지는 확정하지 않았다.

- 판정값은 정수 0/1인지 엄격히 검사하되, 근거 번호 오류는 별도로 처리한다.
  근거 번호가 잘못돼도 정상 양성 판정을 보존하고 기존 원문 span 복원 절차를 사용한다.
- 실제 판정 누락만 원래 청크 크기 내에서 재시도하며, 누락 항목을 프롬프트에 명시한다.
  첫 응답의 정상 판정은 유지하고 재시도에서 복구된 항목만 합친다.
- 남은 누락 항목은 0으로 보완한 뒤 기존 규칙을 적용한다. 로그의 `누락셀`,
  `재시도공고수`, `보완공고수`로 확인한다. 이 보완은 누락 항목의 재현율을 낮출 수 있다.
- 두 응답 모두에서 유효한 판정을 하나도 얻지 못하거나 모델 실행 자체가 실패한 경우는
  계속 오류로 처리한다. 모델 호출을 건너뛰고 규칙만 실행하지 않는다.

18개 테스트에는 잘못된 근거 번호, 일부 판정 누락, 재시도 결과 병합,
재시도 청크 크기·행 순서, 완전히 무효인 모델 응답을 포함한다.
이는 합성 응답을 이용한 CPU 검증이며 실제 GPU 실행·전체 실행시간·점수 검증은 아니다.
