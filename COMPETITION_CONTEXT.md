# 나라장터 법령 위반사항 모니터링 AI 경진대회 — 에이전트 참고 맥락

마지막 확인: 2026-09-16. 공식 대회 페이지, 배포 README, 항목표 및 현재 코드를 확인하여 작성했다.
변경 가능한 규칙·일정·실행환경은 작업 전에 공식 페이지를 다시 확인한다. 실제 법령 판정은 최신 인터넷 법령이 아니라 **대회에서 배포한 법령 스냅샷**을 기준으로 한다.

## 1. 대회의 목적과 문제 정의

공식 명칭: **나라장터 자체입찰 공고 법령 위반사항 모니터링 AI 경진대회**.

공공기관이 직접 발주하는 입찰공고의 공고문·첨부 문서·나라장터 등록 정보를 종합 검토하여, 위반 소지가 있는 공고를 선별하고 담당자에게 판단 근거를 제공하는 AI 추론 파이프라인을 개발한다. 수작업으로 모든 공고를 상시 검토하기 어려운 문제를 해결하는 것이 목적이다.

공고 1건마다 24개 항목의 위반 여부를 각각 0/1로 판정한다. 하나의 공고에 여러 위반이 동시에 존재할 수 있는 다중 레이블 분류 문제다. 1~23번은 법령·예규·고시에 근거한 항목이고, 24번은 공고서와 나라장터 등록값의 불일치 항목이다.

단순 키워드 탐지가 아니다. 적용계약법, 계약방법, 사업금액, 제품 종류, 지역 계층, 예외 조건 등을 함께 판단해야 한다. 필수 문구가 없는 것을 탐지하는 항목은 문서 누락·절단과 실제 기재 부재를 구분해야 한다.

## 2. 평가 점수의 정확한 의미

리더보드 지표는 **24개 항목의 양성(위반=1) 클래스 F1을 단순 평균한 Macro F1**이다.

```text
TP_i = 항목 i에서 실제 위반을 위반으로 예측한 수
FP_i = 항목 i에서 실제 비위반을 위반으로 예측한 수
FN_i = 항목 i에서 실제 위반을 비위반으로 예측한 수

F1_i = 2 * TP_i / (2 * TP_i + FP_i + FN_i)
Score = (F1_1 + ... + F1_24) / 24
```

- 항목별 빈도와 관계없이 각 항목의 가중치는 같다. 모든 평가 항목에 실제 양성 사례가 존재한다.
- 비위반을 올바르게 맞힌 수(TN)는 이 산식에 직접 들어가지 않는다. 전부 0으로 예측하면 점수는 0이다.
- 점수 0.19792는 정확도 19.792%나 공고의 19.792%를 맞혔다는 뜻이 아니다.
- `v1~v24`만 리더보드 점수에 반영한다. 근거 문구 `e1~e24`는 리더보드 점수에 반영하지 않는다.
- Public은 전체 테스트 **1,853건의 100%**로 평가한다. Private은 대회 종료 시점의 Public Score다.
- 1차 상위 15팀이 2차에 진출한다. 2차에서는 재현·규칙 준수를 검증하며, 정량 80%와 근거 문구 정성평가 20%를 종합해 최종 상위 7팀을 선정한다.

### 현재 알려진 성능

사용자가 2026-09-16에 보고한 `baseline/script.py` 제출 점수: **0.1979200359**.

이 값은 사용자 보고값이며, 이 문서를 작성하면서 제출 내역이나 서버 예측 결과를 직접 확인한 것은 아니다. 총점만으로 항목별 성능, 오탐/누락 비율, 낮은 점수의 원인을 확정할 수 없다. 진단하려면 dev에 대한 실제 모델 예측과 항목별 TP/FP/FN/F1을 확인해야 한다.

## 3. 입력 데이터와 제출 형식

| 자료 | 규모 및 용도 |
|---|---|
| `train_unlabeled.jsonl.gz` | 무라벨 공고 20,000건. 라벨 부재를 비위반으로 해석하면 안 된다. |
| `dev.jsonl.gz`, `dev_labels.csv` | 라벨 예시 200건. 평가 데이터 분포를 대표하지 않는다. |
| `data/test.jsonl.gz` | 로컬 형식 확인용 샘플 10건. 서버에서 실제 평가 입력으로 교체된다. |
| `data/항목표.json` | 24개 항목명, 적용 법령·조문, 부재탐지 여부, 비고. 기준일 2026-07-27. |
| `data/정답스키마_디코딩.json` | 구조화 출력 스키마. 정답 값은 없다. |
| `data/법령패키지/` | 판정 기준 법령·행정규칙 및 고시 자료. |

입력 JSONL 한 줄이 공고 1건이다.

- `id`: 그대로 제출에 사용한다. ID나 평가 건수를 하드코딩하지 않는다.
- `docs`: `doc_id`, `type`, `text`로 구성된 문서 배열. 공고문·규격서·과업지시서·제안요청서·예외공표서 등을 함께 검토한다.
- `meta`: 적용계약법, 계약방법, 예산, 추정가격, 지역·업종 제한, 일정 등 나라장터 등록 정보 21개 필드.
- `dropped_doc_counts`, `input_completeness`: 문서 제외 및 관측 완전성 정보.
- 기관명·주소 등은 익명화되어 있다. 지역 토큰에는 지역 계층 정보가 포함될 수 있다.
- `null`과 `"미입력"`은 다르며, 둘 다 자동으로 `"해당 없음"`을 뜻하지 않는다.

출력은 `output/submission.csv`이며 **49열: `id,v1..v24,e1..e24`**이다. 입력과 동일한 ID 집합 및 행 수를 출력한다.

- `v`: 0 또는 1. 확률이나 빈칸을 제출하지 않는다.
- `e`: 해당 공고의 제공 문서에 실제로 존재하는 부분문자열, 최대 500자. 요약·수정 문구나 법령 원문을 대신 인용하지 않는다. 메타데이터는 근거 원문 대조 대상이 아니다.
- 비위반의 근거는 빈칸. 부재탐지형 **v10, v11, v16, v18, v20**의 근거도 항상 빈칸이며 이 5개는 근거 정성평가 대상에서 제외된다.
- UTF-8, BOM 없음, NFC 정규화, CSV 인용 규칙 적용. 근거 문구가 `=`, `+`, `@`로 시작하지 않도록 처리한다.

## 4. 검토 항목 개요

아래는 빠른 탐색용 요약이다. **항목명만으로 위반 조건을 완성하거나 추정하지 말고 항목표의 법령 매핑과 배포 법령의 적용 조건·예외를 확인한다.**

| 항목 | 검토 내용 |
|---|---|
| v1 | 참가자격 특정기관 제한 |
| v2 | 고시금액 미만 실적제한 |
| v3 | 실적제한 1배수 이상 — 사업예산 기준 |
| v4 | 고시금액 이상 특정기관·특정실적 제한 |
| v5 | 고시금액 이상 지역제한 |
| v6 | 고시금액 미만 시·군·구 지역제한 |
| v7 | 고시금액 미만 지역제한 인접 확대 |
| v8 | 실적과 지역의 중복제한 |
| v9 | 과업지시서 특정 모델명 명시 |
| v10 | 중소기업자간 경쟁제품 입찰의 직접생산 관련 요건 부재 |
| v11 | 중소기업자간 경쟁제품 입찰의 중소기업 관련 요건 부재 |
| v12 | 일반제품 직접생산 제한 |
| v13 | 중소기업자간 경쟁제품의 소기업·소상공인 제한 |
| v14 | 고시금액 이상 일반물품 중소기업 제한 |
| v15 | 1억 이상·고시금액 미만 소기업 제한 |
| v16 | 1억 이상·고시금액 미만 중소기업 제한 부재 |
| v17 | 1억원 미만 일반물품 중소기업 제한 |
| v18 | 1억원 미만 일반물품 소기업 제한 부재 |
| v19 | 물품공급 확약서 입찰 시 제출 |
| v20 | SW 사업금액에 따른 입찰참가자격 관련 필수 기재 부재 |
| v21 | 공동계약 지분율 관련 조건 — 항목표 명칭: 공동 5% (10%) |
| v22 | 현장설명회 참석업체로 자격 제한 — 협상계약만 적용 |
| v23 | 현장설명회 공고기간 — 지방계약법 적용 협상계약만 적용 |
| v24 | 공고서와 나라장터 입력값 불일치 — 예산·계약방법·지역제한·업종 등 |

## 5. 개발·실행 제약

- 전 참가자 공통 고정 LLM: `google/gemma-4-26B-A4B-it`. 보조 임베딩 모델: `BAAI/bge-m3`.
- 파인튜닝(LoRA·QLoRA 포함) 및 학습된 모델 가중치·어댑터 제출·로드는 허용되지 않는다. 추론 파이프라인 설계가 중심인 대회다.
- 서버의 `PPS_MODEL_DIR`, `PPS_EMBED_DIR`, `PPS_DATA_DIR`, `PPS_OUTPUT_DIR` 경로를 사용한다.
- 코드 ZIP을 제출하면 서버가 실제 평가 데이터를 대상으로 실행한다. ZIP 최상위에 `script.py`, 선택적으로 `requirements.txt`, 정적 자산용 `model/`을 둔다.
- 고정 모델을 vLLM offline API로 직접 로드한다. 공고당 고정 모델 정상 호출 요건 등 세부 규칙을 준수해야 한다.
- GPU NVIDIA L40S 1개, 가용 VRAM 약 44.7GiB, 7 vCPU, RAM 60GiB.
- 설치 10분, 코드 전체 실행 2시간 제한. 모델 로드·초기화도 실행시간에 포함된다.
- 실행 중 인터넷 차단. 설치 단계에서만 네트워크 사용 가능.
- 모델 컨텍스트 상한 32,768 토큰. 현재 베이스라인은 16,384로 설정한다.
- `vllm`, `torch`, `transformers`, `xgrammar` 등 서버 고정 패키지를 임의 버전으로 변경하지 않는다.
- 제공 자료 외부의 공고 원문·메타데이터 수집, 익명 식별자 복원, 비공개 평가 데이터를 이용한 추가 학습·Pseudo-Labeling은 금지된다.
- 자가 라벨링을 설계할 때는 공식 규칙의 허용 범위를 별도로 확인한다. 무라벨 자료가 있다고 모델 학습이 허용되는 것은 아니다.
- 일일 제출 최대 1회. ZIP 2GB, 압축 해제 후 8GB 제한.

## 6. 현재 코드 상태와 해석상의 주의점

현재 `baseline/script.py`는 법령 BM25 검색을 결합한 추론 파이프라인이다.
```text
입력 로드 → 배포 법령 색인 생성 → 공고별 법령 검색
→ 항목 목록·메타·문서·검색 법령으로 프롬프트 구성
→ 고정 LLM의 구조화 JSON 출력 → 파싱·근거 원문 검증
→ submission.csv 저장·형식 검증
```

기본 설정:

- 공고·첨부 문서 입력 예산 4,000자. 공고문을 먼저 넣으며, 첫 문서는 절단될 수 있고 후속 문서는 예산 초과 시 제외된다.
- 공고문 앞 3,000자를 검색 질의로 사용. 법령 조각 최대 8개, 총 8,000자.
- RAG는 `법령패키지/법령/*.txt`를 색인한다. 법령패키지의 모든 고시 자료까지 검색한다고 가정하지 않는다.
- 24개 항목을 한 번에 판정한다. 시스템 프롬프트에는 항목명·비고·부재탐지 표시가 들어가며 항목별 법령 매핑 전체를 직접 넣지는 않는다.
- 컨텍스트 16,384토큰, 출력 예산 1,536토큰. 입력 초과 시 검색 법령부터 줄이고 필요하면 공고 분량도 줄인다.
- 판단이 어려우면 0으로 출력하도록 지시한다. 출력 파싱 시 빠진 항목도 0으로 보완한다.
- 원문과 불일치하는 근거는 빈칸으로 만들지만, 이 이유만으로 위반 예측을 0으로 바꾸지는 않는다.

**점수 저하 원인으로 조사할 후보**는 문서 절단/첨부 누락, 항목별 법령 검색 누락, 적용 조건·예외 판단 오류, 보수적인 0 판정, 출력 토큰 부족이나 파싱 실패다. 코드상 위험 요인이며 실제 원인으로 검증된 것은 아니다.

`--mock`은 모델 응답을 전부 비위반으로 대체한다. 형식·검색·프롬프트 흐름 검증용이며 예측 성능이나 실제 GPU 실행시간을 검증하지 않는다. `RAG_NOTES.md`의 기존 테스트 통과 기록도 실제 F1 개선 증거로 해석하지 않는다.

성능 개선 작업 시 먼저 실제 dev 예측을 확보하고 항목별 F1·TP·FP·FN, 양성 예측 수, JSON 실패 수, 문서 누락을 확인한다. dev 200건의 점수를 평가 데이터 성능으로 그대로 일반화하지 않는다.

## 7. 참고 자료

- [공식 대회 개요](https://dacon.io/competitions/official/236754/overview/description)
- [공식 평가 기준·서버 환경·제출 안내](https://dacon.io/competitions/official/236754/overview/evaluation)
- [공식 규칙](https://dacon.io/competitions/official/236754/overview/rules)
- [공식 RAG 코드 공유 예제](https://dacon.io/competitions/official/236754/codeshare/14155)
- [배포 데이터 README](DACON_README.md)
- [항목표](data/항목표.json)
- [현재 추론 코드](baseline/script.py)
- [기존 RAG 구현·검증 기록](RAG_NOTES.md)

이 문서는 대회 맥락과 현재 상태를 공유하기 위한 참고 자료다. 숨겨진 정답이나 미확인 성능 분석 결과를 포함하지 않는다.
